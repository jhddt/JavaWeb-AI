<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.ClazzMapper">
    <!--修改员工数据-->
    <update id="updateClazzData">
        update clazz
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="room != null and room != ''">
                room = #{room},
            </if>
            <if test="beginDate != null">
                begin_date = #{beginDate},
            </if>
            <if test="endDate != null">
                end_date = #{endDate},
            </if>
            <if test="masterId != null">
                master_id = #{masterId},
            </if>
            <if test="subject != null">
                subject = #{subject},
            </if>
            update_time = #{updateTime}
        </set>
        where id = #{id}
    </update>


    <!--根据各种条件查询班级的基本信息-->
    <select id="list" resultType="com.itheima.pojo.Clazz">
        select c.*, e.name masterName
        from clazz c left join emp e on c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                c.name like concat('%',#{name},'%')
            </if>
            <if test="begin != null and end != null">
                and c.end_date between #{begin} and #{end}
            </if>
        </where>
        order by c.update_time desc
    </select>

    <!--根据主键ID查询班级的信息-->
    <select id="getById" resultType="com.itheima.pojo.Clazz">
        select c.id, c.name, c.room, c.begin_date, c.end_date, c.master_id, c.subject, c.create_time, c.update_time, e.name masterName
        from clazz c left join emp e on c.master_id = e.id
        where c.id = #{id}
    </select>

    <!--查询所有班级信息-->
    <select id="selectAll" resultType="com.itheima.pojo.Clazz">
        select id, name, room, begin_date, end_date, master_id, subject, create_time, update_time
        from clazz
    </select>

    <!--统计每个班级的人数信息-->
    <select id="studentCountData" resultType="java.util.Map">
        select c.name clazzName, count(s.id) num
        from clazz c left join student s on c.id = s.clazz_id
        group by c.id;
    </select>
</mapper>