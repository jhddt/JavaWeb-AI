<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.UserMapper">

    <!-- 定义resultMap来明确指定字段与属性的映射关系 -->
    <resultMap id="UserResultMap" type="com.itheima.pojo.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
    </resultMap>

    <!-- 使用resultMap替代resultType -->
    <select id="findAll" resultMap="UserResultMap">
        select id, username, password, name, age
        from user
    </select>

    <delete id="deleteById">
        delete
        from user
        where id = #{id}
    </delete>

    <insert id="insert">
        insert into user (username, password, name, age)
        values (#{username}, #{password}, #{name}, #{age})
    </insert>

    <update id="update">
        update user
        set username = #{username}, password = #{password}, name = #{name}, age = #{age}
        where id = #{id}
    </update>

    <select id="findByUsernameAndPassword" resultMap="UserResultMap">
        select id, username, password, name, age
        from user
        where username = #{username} and password = #{password}
    </select>

</mapper>